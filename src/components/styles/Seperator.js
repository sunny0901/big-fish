import { silverColor } from '../../constants'

const styles = {
    line: {
        width: 900,
        borderBottom: '1px solid' + silverColor,
        marginTop: 21,
        marginBottom: 21
    }
}

export default styles;