import {
    themeColor,
}from '../../constants';

const styles = {
    button: {
        width: 386,
        height: 50,
        backgroundColor: themeColor,
        borderRadius: 5,
        border: 'none',

        fontSize: 25,
        color: 'white',
    }
};

export default styles;