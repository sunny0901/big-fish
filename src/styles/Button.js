import {
    themeColor,
}from '../constants';

const styles = {
    button: {
        width: 386,
        height: 50,
        backgroundColor: themeColor,
        borderRadius: 5,
        border: 'none',

        fontSize: 25,
        color: 'white',

        marginTop: 89.5,
        marginBottom: 52,
    }
};

export default styles;